# Copy to .env at the project root — read by docker-compose.yml.
# (Not to be confused with backend/.env or frontend/.env.local, which are
# used for local, non-Docker development.)

MODEL_WEIGHTS_PATH=/app/ml/runs/train/tumor_yolo11/weights/best.pt
PUBLIC_API_BASE_URL=http://localhost:8000
